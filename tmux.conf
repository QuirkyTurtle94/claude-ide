# claude-ide tmux config
# Copy to ~/.tmux.conf or append to your existing config

# Mouse support (clickable/resizable panes)
set -g mouse on

# Status bar hint
set -g status 2
set -g status-format[1] "#[align=centre,fg=#888888,bg=#1a1a1a] Ctrl+b then : → kill-session to exit "
